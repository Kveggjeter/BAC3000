import {SetMarkerProps} from "./SetMarkerProps.ts";

/**
 * Type casting for MapTools.tsx
 * SetMarkers is from another typing, @see SetMarkerProps
 */
export interface MapToolsProps {
    setMarkers: SetMarkerProps;
}